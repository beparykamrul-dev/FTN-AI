# FTN Kismet site policy.
# Kismet is an authorized wireless telemetry sensor for FTN-owned/managed networks.
# Do not enable collection on networks or devices without authorization.
# Keep Kismet logs local; forward only approved metadata to FTN observability.

# Keep the web service bound for local reverse-proxy integration.
httpd_bind_address=127.0.0.1
